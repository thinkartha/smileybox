# API URL (backend). For local dev, use http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

# Backend uses DynamoDB. AWS credentials must be configured.
# Optional: JWT_SECRET, FRONTEND_URL, USERS_TABLE, etc.
